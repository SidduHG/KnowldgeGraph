fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.1
pydantic-settings==2.6.1
sqlalchemy[asyncio]==2.0.36
aiomysql==0.2.0
pymysql==1.1.1
httpx==0.27.2
python-dotenv==1.0.1
mcp==1.2.0

# ── Optional (semantic search) ─────────────────────────────────────────────
# Remove these two lines if you don't need search_semantic.
# ~500MB install; ~90MB runtime footprint on first query.
sentence-transformers==3.3.1
numpy==2.2.0
# tiktoken is used by the token-budget enforcer (cheap, ~2MB).
tiktoken==0.8.0
